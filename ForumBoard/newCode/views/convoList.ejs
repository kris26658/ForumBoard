<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Conversations</title>
</head>

<body>
    <div class="oldConvo">
        <p>Choose from an existing conversation...</p>
        <form action="/chat" method="get">
            <% convos.forEach(convo=> { %>
                <button id="convoButton" class="convoTitle" type="submit" name="title" value="<%= convo.title %>">
                    <%= convo.title %>
                </button>
            <% }); %>
        </form>
    </div>

    <div class="newConvo">
        <p>...or create a new one!</p>
        <form action="/convoList" method="post">
            <label for="newConvoTitle">Conversation Title:</label>
            <input id="newConvoTitle" name="convoTitle" class="convoTitle" type="text" placeholder="Add a title for the new conversation" size="70" required>
            <button type="submit">Create</button>
        </form>
    </div>
</body>

</html>